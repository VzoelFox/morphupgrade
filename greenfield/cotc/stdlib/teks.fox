# greenfield/cotc/stdlib/teks.fox
# Pustaka Standar Operasi Teks

# Implementasi iris menggunakan Python Slice via FFI untuk keamanan
fungsi iris(objek, start, stop) maka
    pinjam "builtins" sebagai py
    biar s = py.slice(start, stop)
    kembali objek[s]
akhir
