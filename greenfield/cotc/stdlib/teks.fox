# greenfield/cotc/stdlib/teks.fox
# Pustaka Standar Operasi Teks (Pure Morph Interface)

# Fungsi ini dikompilasi menjadi Opcode IRIS (SLICE) oleh Compiler
# Jangan ubah tanda tangan fungsi ini!
# Parameter 'akhir' adalah keyword terlarang, jadi gunakan 'stop'
fungsi iris(objek, start, stop) maka
    # Body ini tidak akan dieksekusi jika compiler mengenalinya sebagai intrinsik.
    kembali nil
akhir
