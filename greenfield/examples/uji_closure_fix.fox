fungsi outer() maka
    biar x = 5
    fungsi inner() maka
        ubah x = 10
        tulis(x)
    akhir
    inner()
    tulis(x)
    jika x == 10 maka
        tulis("[LULUS] Closure variable modified")
    lain
        tulis("[GAGAL] Closure variable not modified")
    akhir
akhir

fungsi utama() maka
    outer()
akhir
