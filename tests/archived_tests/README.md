# Archived Tests

Tests di folder ini adalah tests yang sudah tidak relevan dengan
implementasi current tapi disimpan untuk referensi masa depan.

- `test_monitor_sumber_daya.py`: FASE-3A async resource monitoring
- `compiler/`: Seluruh direktori tes untuk proyek `morphupgrade` (compiler/transpiler) yang sudah usang.
- `test_parser_recovery.py`: Tes pemulihan parser yang bergantung pada `morphupgrade`.
