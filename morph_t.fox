// morph/morph_t.fox
// Definisi tipe inti untuk compiler Morph yang ditulis dalam Morph.
// Langkah pertama adalah mendefinisikan apa itu 'Token'.

kelas Token maka
  fungsi inisiasi(tipe, nilai, baris, kolom) maka
    ini.tipe = tipe;
    ini.nilai = nilai;
    ini.baris = baris;
    ini.kolom = kolom;
  akhir
akhir
