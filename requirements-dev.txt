# Production dependencies
# (none for now - Morph is pure Python)

# Development dependencies
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-timeout>=2.1.0
pytest-xdist>=3.3.0  # Parallel test execution
llvmlite>=0.41.0
psutil>=5.9.0
pytest-asyncio
