(* Dihasilkan oleh Transpiler MORPH-ke-OCaml *)
let _ = print_endline ("Halo dari MORPH!")
let _ = print_endline (string_of_int 123)
let a = 10
let b = 25
let c = (a + (b * 2))
let _ = print_endline (string_of_int c)
let PI = 3.14
let _ = print_endline (string_of_int PI)
let _ = if (c > 50) then (
let _ = print_endline ("c lebih besar dari 50")
)
