// File: teks.fox
// Deskripsi: Modul standard library untuk operasi dan utilitas string.
// Menggunakan FFI untuk memanggil fungsi helper di _teks_internal.py

pinjam "transisi.stdlib.wajib._teks_internal" sebagai internal

fungsi pisah(teks, pembatas) maka
    kembali internal.pisah(teks, pembatas)
akhir

fungsi gabung(daftar, penghubung) maka
    kembali internal.gabung(daftar, penghubung)
akhir

fungsi potong_spasi(teks) maka
    kembali internal.potong_spasi(teks)
akhir

fungsi huruf_besar(teks) maka
    kembali internal.huruf_besar(teks)
akhir

fungsi huruf_kecil(teks) maka
    kembali internal.huruf_kecil(teks)
akhir

fungsi ganti(teks, cari, ganti_dengan) maka
    kembali internal.ganti(teks, cari, ganti_dengan)
akhir

fungsi mulai_dengan(teks, awalan) maka
    kembali internal.mulai_dengan(teks, awalan)
akhir

fungsi berakhir_dengan(teks, akhiran) maka
    kembali internal.berakhir_dengan(teks, akhiran)
akhir

fungsi panjang(teks) maka
    kembali internal.panjang(teks)
akhir

fungsi potong(teks, awal, akhir) maka
    kembali internal.potong(teks, awal, akhir)
akhir
